<%= form_for(@workorder) do |f| %>
  <% if @workorder.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@workorder.errors.count, "error") %> prohibited this workorder from being saved:</h2>

      <ul>
      <% @workorder.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


	<table>
		<tr>
			<th>KEY</th>
			<th>VALUE</th>
		</tr>

		<tr>
			<td>Process Mode</td>
			<td><%= f.select(:procmode_id, @procmodes.map{|t|[t.name, t.id]}) %></td>
		</tr>

		<tr>
			<td>Data Set</td>
			<td><%= f.text_field :data_set %></td>
		</tr>

		<tr>
			<td></td>
			<td><%= f.submit %></td>
		</tr>
	</table>
<% end %>
