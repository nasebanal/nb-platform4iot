<%= form_for(@procmode) do |f| %>
  <% if @procmode.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@procmode.errors.count, "error") %> prohibited this procmode from being saved:</h2>

      <ul>
      <% @procmode.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
